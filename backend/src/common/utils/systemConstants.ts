export enum HttpStatus {
  NOT_FOUND = 404,
  OK = 200,
  CREATED = 201,
  NO_CONTENT = 204,
  SERVER_ERROR = 500,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
}

const BCRYPT_SALT_ROUNDS = 10;

export { BCRYPT_SALT_ROUNDS };
